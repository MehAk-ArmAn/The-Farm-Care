@extends('admin.layout') @section('heading','Admin Profile') @section('back_url', route('admin.dashboard')) @section('back_label','Dashboard') @section('content')<form class="panel" method="post" action="{{ route('admin.profile.update') }}">@csrf @method('PUT')<div class="grid2"><div class="field"><label>Name</label><input name="name" value="{{ old('name',auth()->user()->name) }}" required></div><div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email',auth()->user()->email) }}" required></div><div class="field"><label>Current Password</label><input type="password" name="current_password"><span class="hint">Required only when changing password.</span></div><div></div><div class="field"><label>New Password</label><input type="password" name="password"></div><div class="field"><label>Confirm New Password</label><input type="password" name="password_confirmation"></div><div class="field full"><button class="btn btn-primary">Update Profile</button></div></div></form>@endsection