@extends('admin.layout') @section('heading',ucfirst($inquiry->type).' #'.$inquiry->id) @section('back_url', route('admin.inquiries.index')) @section('back_label','Inquiries & Quotes') @section('content')<div class="grid2"><div class="panel" style="margin-top:0"><h2>Customer Request</h2><p><strong>Name:</strong> {{ $inquiry->name }}</p><p><strong>Email:</strong> <a href="mailto:{{ $inquiry->email }}">{{ $inquiry->email }}</a></p><p><strong>Phone:</strong> {{ $inquiry->phone ?: '—' }}</p><p><strong>Company:</strong> {{ $inquiry->company ?: '—' }}</p><p><strong>Country:</strong> {{ $inquiry->country ?: '—' }}</p><p><strong>Product:</strong> {{ $inquiry->product?->name ?: '—' }}</p><p><strong>Variant / Model:</strong> {{ $inquiry->variant ?: '—' }}</p><p><strong>Quantity:</strong> {{ $inquiry->quantity ?: '—' }}</p><p><strong>Subject:</strong> {{ $inquiry->subject ?: '—' }}</p><hr style="border:0;border-top:1px solid #eee"><p style="white-space:pre-line">{{ $inquiry->message }}</p></div><form class="panel" style="margin-top:0" method="post" action="{{ route('admin.inquiries.update',$inquiry) }}">@csrf @method('PUT')<div class="field"><label>Status</label><select name="status">@foreach(['new','in_progress','answered','closed'] as $s)<option value="{{ $s }}" @selected($inquiry->status===$s)>{{ ucfirst(str_replace('_',' ',$s)) }}</option>@endforeach</select></div><div class="field" style="margin-top:14px"><label>Internal Admin Notes</label><textarea name="admin_notes" style="min-height:220px">{{ $inquiry->admin_notes }}</textarea></div><button class="btn btn-primary" style="margin-top:14px">Update Request</button></form></div>@endsection